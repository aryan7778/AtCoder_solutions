#include<bits/stdc++.h>
#include<cctype>
#define fastIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using namespace std;

int main(){
	fastIO
  char ch;
  cin>>ch;
  if(isupper(ch))
    cout<<'A'<<endl;
  else
    cout<<'a'<<endl;
  return 0;
}
